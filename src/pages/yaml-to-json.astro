---
import ToolLayout from '@/layouts/ToolLayout.astro';
import YamlToJson from '@/components/tools/YamlToJson.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('yaml-to-json')!;
const relatedTools = getToolsByCategory('converters').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <YamlToJson client:load />

  <div slot="seo-content">
    <h2>What is YAML to JSON Converter?</h2>
    <p>
      Convert YAML to JSON format and vice versa.
    </p>
  </div>

  <div slot="related-tools" class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
  </div>
</ToolLayout>

