---
import ToolLayout from '@/layouts/ToolLayout.astro';
import PercentEncoder from '@/components/tools/PercentEncoder.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('percent-encoder')!;
const relatedTools = getToolsByCategory('encoders').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <PercentEncoder client:load />

  <Fragment slot="seo-content">
    <h2>What is Percent Encoder/Decoder?</h2>
    <p>
      Encode text to percent-encoded format (URL encoding) or decode percent-encoded strings back to text.
    </p>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>

