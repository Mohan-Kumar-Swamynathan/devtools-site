---
import ToolLayout from '@/layouts/ToolLayout.astro';
import HtmlEncoder from '@/components/tools/HtmlEncoder.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('html-encoder')!;
const relatedTools = getToolsByCategory('encoders').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <HtmlEncoder client:load />

  <Fragment slot="seo-content">
    <h2>What is HTML Encoder/Decoder?</h2>
    <p>
      Convert special characters to HTML entities and decode HTML entities back to characters.
    </p>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>

