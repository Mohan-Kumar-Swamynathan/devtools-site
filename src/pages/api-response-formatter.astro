---
import ToolLayout from '@/layouts/ToolLayout.astro';
import ApiResponseFormatter from '@/components/tools/ApiResponseFormatter.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('api-response-formatter')!;
const relatedTools = getToolsByCategory('network').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <ApiResponseFormatter client:load />

  <div slot="seo-content">
    <h2>What is API Response Formatter?</h2>
    <p>
      Format API responses in JSON or XML format with proper indentation. Make API responses readable.
    </p>
  </div>

  <div slot="related-tools" class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
  </div>
</ToolLayout>

