---
import ToolLayout from '@/layouts/ToolLayout.astro';
import XmlValidator from '@/components/tools/XmlValidator.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('xml-validator')!;
const relatedTools = getToolsByCategory('validators').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <XmlValidator client:load />

  <Fragment slot="seo-content">
    <h2>What is XML Validator?</h2>
    <p>
      Validate XML syntax and structure. Check for well-formed XML documents.
    </p>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>

