---
import ToolLayout from '@/layouts/ToolLayout.astro';
import DockerfileGenerator from '@/components/tools/DockerfileGenerator.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('dockerfile-generator')!;
const relatedTools = getToolsByCategory('generators').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <DockerfileGenerator client:load />

  <div slot="seo-content">
    <h2>What is Dockerfile Generator?</h2>
    <p>
      Generate Dockerfiles with common configurations for Node.js, Python, Go, Rust, PHP, and more.
    </p>
  </div>

  <div slot="related-tools" class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
  </div>
</ToolLayout>

