---
import ToolLayout from '@/layouts/ToolLayout.astro';
import StringEscape from '@/components/tools/StringEscape.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('string-escape')!;
const relatedTools = getToolsByCategory('encoders').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <StringEscape client:load />

  <Fragment slot="seo-content">
    <h2>What is String Escape/Unescape?</h2>
    <p>
      Escape special characters in strings or unescape escaped strings.
    </p>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>

