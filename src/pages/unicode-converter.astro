---
import ToolLayout from '@/layouts/ToolLayout.astro';
import UnicodeConverter from '@/components/tools/UnicodeConverter.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('unicode-converter')!;
const relatedTools = getToolsByCategory('encoders').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <UnicodeConverter client:load />

  <div slot="seo-content">
    <h2>What is Unicode Converter?</h2>
    <p>
      Convert text to Unicode escape sequences and decode Unicode back to text.
    </p>
  </div>

  <div slot="related-tools" class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
  </div>
</ToolLayout>

