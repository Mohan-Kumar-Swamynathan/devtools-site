---
import ToolLayout from '@/layouts/ToolLayout.astro';
import CorsChecker from '@/components/tools/CorsChecker.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('cors-checker')!;
const relatedTools = getToolsByCategory('network').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <CorsChecker client:load />

  <div slot="seo-content">
    <h2>What is CORS Checker?</h2>
    <p>
      Check if a URL has CORS enabled and view CORS headers. Useful for debugging cross-origin requests.
    </p>
  </div>

  <div slot="related-tools" class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
  </div>
</ToolLayout>

