---
import ToolLayout from '@/layouts/ToolLayout.astro';
import ErrorDecoder from '@/components/tools/ErrorDecoder.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('error-decoder')!;
const relatedTools = getToolsByCategory('code').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <ErrorDecoder client:load />

  <Fragment slot="seo-content">
    <h2>What is Error Decoder?</h2>
    <p>
      Decode error messages and stack traces. Extract useful information from error logs to help with debugging.
    </p>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>

