---
import ToolLayout from '@/layouts/ToolLayout.astro';
import JsonPath from '@/components/tools/JsonPath.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('json-path')!;
const relatedTools = getToolsByCategory('json').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <JsonPath client:load />

  <Fragment slot="seo-content">
    <h2>What is JSON Path Finder?</h2>
    <p>
      Find and extract values from JSON using JSONPath expressions. 
      Navigate complex JSON structures easily.
    </p>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>

