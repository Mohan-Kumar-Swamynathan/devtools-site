---
import ToolLayout from '@/layouts/ToolLayout.astro';
import RegexCheatSheet from '@/components/tools/RegexCheatSheet.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('regex-cheat-sheet')!;
const relatedTools = getToolsByCategory('code').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <RegexCheatSheet client:load />

  <Fragment slot="seo-content">
    <h2>What is Regex Cheat Sheet?</h2>
    <p>
      Quick reference for regular expression patterns, quantifiers, anchors, groups, and flags.
    </p>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>

