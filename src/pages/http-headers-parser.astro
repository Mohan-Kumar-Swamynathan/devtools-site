---
import ToolLayout from '@/layouts/ToolLayout.astro';
import HttpHeadersParser from '@/components/tools/HttpHeadersParser.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('http-headers-parser')!;
const relatedTools = getToolsByCategory('network').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <HttpHeadersParser client:load />

  <Fragment slot="seo-content">
    <h2>What is HTTP Headers Parser?</h2>
    <p>
      HTTP Headers Parser is a free online tool that parses and formats HTTP request and response headers. 
      View headers in a readable format.
    </p>
    
    <h2>Features</h2>
    <ul>
      <li>Parse HTTP headers</li>
      <li>Format headers for readability</li>
      <li>Export as JSON</li>
      <li>100% client-side</li>
    </ul>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>
