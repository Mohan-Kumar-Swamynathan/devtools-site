---
import ToolLayout from '@/layouts/ToolLayout.astro';
import CurlConverter from '@/components/tools/CurlConverter.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('curl-converter')!;
const relatedTools = getToolsByCategory('misc').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <CurlConverter client:load />

  <Fragment slot="seo-content">
    <h2>What is cURL Converter?</h2>
    <p>
      Convert cURL commands to code in various programming languages.
    </p>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>

