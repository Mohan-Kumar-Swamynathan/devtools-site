---
import ToolLayout from '@/layouts/ToolLayout.astro';
import ColorPicker from '@/components/tools/ColorPicker.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('color-picker')!;
const relatedTools = getToolsByCategory('css').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <ColorPicker client:load />

  <div slot="seo-content">
    <h2>What is Color Picker?</h2>
    <p>
      Pick colors and get them in multiple formats: HEX, RGB, RGBA, HSL, HSLA. Perfect for web development.
    </p>
  </div>

  <div slot="related-tools" class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
  </div>
</ToolLayout>

