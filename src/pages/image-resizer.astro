---
import ToolLayout from '@/layouts/ToolLayout.astro';
import ImageResizer from '@/components/tools/ImageResizer.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('image-resizer')!;
const relatedTools = getToolsByCategory('image').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <ImageResizer client:load />

  <Fragment slot="seo-content">
    <h2>What is Image Resizer?</h2>
    <p>
      Resize images to custom dimensions. Supports maintaining aspect ratio. All processing happens in your browser.
    </p>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>

