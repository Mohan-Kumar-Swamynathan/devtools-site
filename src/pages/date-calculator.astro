---
import ToolLayout from '@/layouts/ToolLayout.astro';
import DateCalculator from '@/components/tools/DateCalculator.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('date-calculator')!;
const relatedTools = getToolsByCategory('time').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <DateCalculator client:load />

  <div slot="seo-content">
    <h2>What is Date Calculator?</h2>
    <p>
      Add or subtract days, weeks, months, or years from a date. Useful for date arithmetic.
    </p>
  </div>

  <div slot="related-tools" class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
  </div>
</ToolLayout>

