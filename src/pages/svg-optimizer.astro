---
import ToolLayout from '@/layouts/ToolLayout.astro';
import SvgOptimizer from '@/components/tools/SvgOptimizer.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('svg-optimizer')!;
const relatedTools = getToolsByCategory('image').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <SvgOptimizer client:load />

  <Fragment slot="seo-content">
    <h2>What is SVG Optimizer?</h2>
    <p>
      Optimize SVG code by removing unnecessary whitespace, comments, and attributes. Reduce file size.
    </p>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>

