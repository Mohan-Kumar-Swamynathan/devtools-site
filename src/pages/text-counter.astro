---
import ToolLayout from '@/layouts/ToolLayout.astro';
import TextCounter from '@/components/tools/TextCounter.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('text-counter')!;
const relatedTools = getToolsByCategory('text').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <TextCounter client:load />

  <Fragment slot="seo-content">
    <h2>What is Text Counter?</h2>
    <p>
      Count words, characters, sentences, paragraphs, and lines in your text.
    </p>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>

