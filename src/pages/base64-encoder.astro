---
import ToolLayout from '@/layouts/ToolLayout.astro';
import Base64Encoder from '@/components/tools/Base64Encoder.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('base64-encoder')!;
const relatedTools = getToolsByCategory('encoders').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <Base64Encoder client:load />

  <div slot="seo-content">
    <h2>What is Base64 Encoder/Decoder?</h2>
    <p>
      Encode text to Base64 or decode Base64 to text. Fast and secure encoding/decoding.
    </p>
  </div>

  <div slot="related-tools" class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
  </div>
</ToolLayout>

