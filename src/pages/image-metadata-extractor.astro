---
import ToolLayout from '@/layouts/ToolLayout.astro';
import ImageMetadataExtractor from '@/components/tools/ImageMetadataExtractor.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('image-metadata-extractor')!;
const relatedTools = getToolsByCategory('image').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <ImageMetadataExtractor client:load />

  <Fragment slot="seo-content">
    <h2>What is Image Metadata Extractor?</h2>
    <p>
      Extract metadata from images including dimensions, file size, type, and modification date.
    </p>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>

