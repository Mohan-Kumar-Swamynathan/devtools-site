---
import ToolLayout from '@/layouts/ToolLayout.astro';
import NumberBaseConverter from '@/components/tools/NumberBaseConverter.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('number-base')!;
const relatedTools = getToolsByCategory('converters').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <NumberBaseConverter client:load />

  <Fragment slot="seo-content">
    <h2>What is Number Base Converter?</h2>
    <p>
      Convert between binary, decimal, hexadecimal, and octal number systems.
    </p>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>

