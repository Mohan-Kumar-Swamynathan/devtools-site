---
import ToolLayout from '@/layouts/ToolLayout.astro';
import HtaccessGenerator from '@/components/tools/HtaccessGenerator.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('htaccess-generator')!;
const relatedTools = getToolsByCategory('misc').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <HtaccessGenerator client:load />

  <Fragment slot="seo-content">
    <h2>What is .htaccess Generator?</h2>
    <p>
      Generate .htaccess configuration rules for Apache servers.
    </p>
  </Fragment>

  <Fragment slot="related-tools">
    <div class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
    </div>
  </Fragment>
</ToolLayout>

