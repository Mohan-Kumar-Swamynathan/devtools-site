---
import ToolLayout from '@/layouts/ToolLayout.astro';
import WordFrequencyCounter from '@/components/tools/WordFrequencyCounter.tsx';
import ToolCard from '@/components/common/ToolCard.astro';
import { getToolBySlug, getToolsByCategory } from '@/lib/tools';

const tool = getToolBySlug('word-frequency-counter')!;
const relatedTools = getToolsByCategory('text').filter(t => t.id !== tool.id).slice(0, 4);
---

<ToolLayout tool={tool}>
  <WordFrequencyCounter client:load />

  <div slot="seo-content">
    <h2>What is Word Frequency Counter?</h2>
    <p>
      Word Frequency Counter is a free online tool that counts word frequencies in text. 
      Analyze which words appear most often.
    </p>
    
    <h2>Features</h2>
    <ul>
      <li>Count word frequencies</li>
      <li>Visual frequency bars</li>
      <li>Sort by frequency</li>
      <li>Total and unique word counts</li>
      <li>100% client-side</li>
    </ul>
  </div>

  <div slot="related-tools" class="grid grid-cols-1 xs:grid-cols-2 md:grid-cols-4 gap-4">
      {relatedTools.map(t => <ToolCard {...t} />)}
  </div>
</ToolLayout>
